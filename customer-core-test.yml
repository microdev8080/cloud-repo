Spring:
  kafka:
    bootstrap-servers: 127.0.0.1:29092
    consumer:
      auto-offset-reset: earliest
      group-id: test-kafka-cluster
spring:
  cloud:
    discovery:
      enabled: 'false'
  redis:
    host: redis
    port: '6370'
  data:
    redis:
      repositories:
        enabled: 'false'
test:
  topic: test_topic
